[package]
name = "quick_strings"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = "0.5.1"
regex = { version = "1.7.0", default-features = false, features = ["std", "unicode", "perf"] }

[[bench]]
name = "main"
harness = false
